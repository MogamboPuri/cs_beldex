# Changelog

All notable changes to this project will be documented in this file.

## 0.1.0 - Initial Release

- Created the `cs_beldex` package based on the structure of `cs_beldex`.
- Set up project using:

```dart create -t package cs_beldex```

- Added initial folder structure under `lib/`:
- `cs_beldex.dart` (public exports)
- `src/` (internal code)
- Added basic enums:
- `Network`
- `TransactionPriority`
- `BeldexSeedType`
- Prepared repository for future development.
